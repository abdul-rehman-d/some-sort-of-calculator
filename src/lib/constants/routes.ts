export enum UserRoutes {
  DASHBOARD = '/dashboard/:userId',
  CLIENTS = '/dashboard/:userId/clients',
  TIMESHEETS = '/dashboard/:userId/timesheets',
  SUMMARY = '/dashboard/:userId/summary',
}

export enum PublicRoutes {
  HOME = '/',
  ABOUT = '/about',
}
